area=lambda r:3.14*r*r
print(area(5))
#lambda function that returns the square of a given number. The function should take one argument, which is the number to be squared. You can use the lambda keyword to create the function and then call it with a sample number to see the result.
square=lambda x:x*x
print(square(4))
